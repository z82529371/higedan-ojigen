# 髭男應援歌詞工具 (higedan-ojigen)

一個 Web App，用於在 Official髭男dism 的歌曲播放時顯示同步歌詞，並標示需要應援（合唱、手勢、拍手）的段落。目前涵蓋 31 首歌曲。

## Language

**應援**:

演唱會現場觀眾的互動動作，包括合唱、手勢與拍手。非資料實體，實際資料以應援點承載。

_Avoid_: 應援行為、互動

**應援點**:

資料檔中實際承載應援的單位，以絕對時間區間（`start`~`end`，或以 `times[]` 指定多組並展開）定義，內含一或多個動作；任何時刻最多屬於一個應援點，時間不得重疊。

_Avoid_: 應援段落、應援區間

**動作**:

應援點內單一的互動指示，型別為合唱／手勢／拍手，可附 `text` 命中歌詞片段。標記由動作即時渲染，非資料檔中獨立儲存的實體。

**間奏**:

無歌詞唱出的樂器段落；應援點落在間奏時，該段標記以獨立標記列顯示。

**行標記列／獨立標記列**:

標記的兩種橫列位置——附著於歌詞行的「行標記列」，與間奏／口號未命中歌詞時使用的「獨立標記列」。

**片段錨定標記**:

有 `text` 且命中歌詞片段時，錨定於該片段正上方的標記（手勢 Emoji）；錨定後自行標記列移除，不重複顯示。

**歌曲**:

一首 Official髭男dism 的樂曲，是資料的頂層單位。目前共有 31 首。

**歌詞**:

歌曲的文字內容，隨播放時間與樂曲對齊顯示。

**歌詞行**:

歌詞的最小顯示與時間單位，有各自的開始/結束時間與文字內容（`text`），可附羅馬拼音（`romaji`）。行界線以斷句／換氣為單位；製作初期以空格（半形/全形）自動分割作為起點，再由人工依斷句調整（英文句子內單字空格不切行）。

- **合唱 (Chorus)**：需要觀眾一同唱出的段落，內容有三種型態：**合唱一整句**（跟著原歌詞整句唱，不需 `text`）、**唱一部分**（以 `text` 指定歌詞中實際唱出的片段並染色，如括號內字句；若命中歌詞則直接對歌詞片段染色，不額外在上方印重複的文字貼紙標記）與**不是唱這句歌詞**（口號/補充文字；若未命中歌詞則維持顯示獨立標記）。
- **手勢 (Gesture)**：手勢動作點，以 emoji 呈現；可為多個 emoji 以半形空格組合（如 `✊ ✊ ✊ ✊`），驗證時僅要求每個組成皆在手勢目錄（`gestures.json`）內。若帶有 `text` 且命中歌詞時，手勢 Emoji 標記會片段錨定在該文字正上方，並從行標記列移除（不重複顯示）。（手勢顯示為 emoji、拍手顯示為拍點字串、合唱顯示補充文字），各染對應應援色。標記由動作即時渲染，非資料檔中獨立儲存的實體。標記分為「行標記列／獨立標記列」與「片段錨定標記」：當手勢動作帶有 `text` 且命中歌詞片段時，手勢 Emoji 標記會精準錨定於該片段正上方呈現，並從行標記列中移除（不重複出現）；當合唱動作帶有 `text` 且命中歌詞片段時，直接對歌詞片段染色，因歌詞已有相同文字故不額外印製重複的文字標記。無 `text` 或整行未命中者維持在行標記列；間奏或未命中口號維持獨立標記列。

**類別標示 (Category Label)**:

歌詞行上以 `(合唱)`/`(手勢)`/`(拍手)` 括號文字標示該行涵蓋的應援類別，塗對應應援色；多類別時依「合唱 > 手勢 > 拍手」順序排列。僅完整歌詞模式顯示；卡拉OK不顯示，類別僅以染色與標記呈現。

**歌詞染色**:

應援動作依類別將歌詞染成對應色（合唱琥珀、手勢暖橘、拍手玫粉），每類別皆有整句（無 `text` 整行染）、部分（`text` 命中歌詞只染片段）、未命中（不染，標記照常）三態；同行多類別時依「合唱 > 手勢 > 拍手」優先序，整行染色勝過部分染色。

**音檔**:

歌曲的實際音樂檔案（優先 .m4a，缺失時自動以 .mp3 備援），由使用者自行提供並放置於 public/audio/ 資料夾中，供 App 播放。歌曲資料的 `audio` 欄位宣告期望的檔名，實際可用路徑由 App 載入時偵測；App 本身不內含或散佈音檔。

**開發對齊模式 (Dev Mode)**:

專門供對齊歌詞時間軸的開發介面，提供時間微調與行鎖定等校對功能。

**一般瀏覽模式 (General Mode)**:

歌迷預設使用的介面，隱藏微調與時間秒數，保持純粹乾淨。

**完整歌詞模式**:

歌詞的靜態閱讀列表，歌詞行與應援標記依時間交錯排列；仍標示目前行並支援點擊跳轉。

**卡拉OK模式**:

同步捲動的播放畫面，跟著播放時間自動對齊、高亮目前行；應援標記常駐顯示，進入應援點區間時亮起。

**行鎖定 (Line Locking)**:

將已校對完成的歌詞行狀態固定（`lockedLines`），防止後續調整其他句子時被連鎖推擠或時間平分演算法改動。屬開發對齊的工作狀態，非歌曲播放資料。

**目前行**:

播放時間落在其時間區間內的那一行歌詞，是捲動對齊與高亮的主軸；與應援點涵蓋的多行標記顯示無關。

**演唱會提示 (note)**:

歌曲的可選說明文字，顯示於播放頁標題下；不一定每首都有。

## 已知決策（session 進行中）

- 產品形式為 **Web App**。

- 聲音來源為 **App 內播放**；音檔由使用者自行準備 31 首，格式統一使用 **.m4a**（系統支援 .m4a/.mp3 相容）。

- 播放時歌詞**邊播放邊滾動**對齊。

- 應援一律以**顏色**標示（合唱 `#d97706` 暖金琥珀、手勢 `#ea580c` 活力暖橘、拍手 `#db2777` 亮紫玫粉）。**歌詞染色**三類動作皆適用，各依三態：**整句**（無 `text`）整行染色、**部分**僅 `text` 對應片段染色（手勢動作之 Emoji 標記錨定於片段正上方，合唱命中則直接染色不印重複文字標記）、**未命中**（`text` 找不到）不染歌詞僅顯示獨立標記；同行多類別時依「合唱 > 手勢 > 拍手」優先序，整行染色勝過部分染色。左條與類別標示 `(合唱)` 在完整歌詞模式恆常顯示，與染色本體並存；卡拉OK不顯示類別標示。

- 播放控制與系統輔助色（--brand）採用 `#b8a024` 經典高雅金；全站字體採用 **Plus Jakarta Sans + Noto Sans JP/TC**。

- 應援點**不得時間重疊**；任何時刻最多屬於一個應援點。支援使用 `times` 陣列（`times: [{ "start": s1, "end": e1 }, ...]`）一次指定多組重複出現的秒數區間，載入展開後進行重疊驗證（同組 `times[]` 內部亦受檢）。單一應援點時間涵蓋多行歌詞時（如廣域拍手或合唱段落），**涵蓋的所有歌詞行皆自動顯示應援標記與常駐染色**（方案 A）。

- 播放時間距離下一個應援點開始剩餘 3 秒內時，顯示 **`⏳3` 倒數**提示歌迷入場；**完整歌詞模式**顯示於該應援區間涵蓋的**第一行（或獨立標記列）的類別標籤旁同排**，**卡拉OK模式**顯示於該**行／列上方**。

- 特殊手勢與拍手以 **emoji** 原生呈現，不做多餘外框或加工。

- 每首**歌曲**的資料**明確引用**其音檔檔名，不依賴檔名慣例。

- 合唱的**補充文字**以獨立標記顯示（塗合唱色），不插入歌詞流；口號的羅馬拼音（`romaji`）以**同色小字**顯示於口號標記下方；若口號命中歌詞，羅馬拼音錨定在**命中片段正下方**（與歌詞分行），完整歌詞與卡拉OK模式皆然。

- **歌詞**以「行」為單位，每行有自己的開始/結束時間。**行界線以斷句／換氣為單位**（一口氣唱完的小段）。遇到空格自動分割為獨立行。

- **資料製作流程**：使用者提供歌詞與應援資訊，AI 負責分行、標時間並組裝成資料檔；分行以空格分割為起點，再由人工依斷句／換氣語意調整。

- **時間標記流程**：使用者提供音檔後，AI 以 ffmpeg 靜音偵測產出候選句界時間，由使用者確認對應歌詞行／應援點，再由 AI 組裝資料檔。

- 每首**歌曲**可選附**演唱會提示**（`note`）文字，於播放頁標題下顯示，**不一定每首都有**。

- 應援點涵蓋的所有歌詞行皆顯示該應援點標記：手勢動作有 `text` 且命中歌詞時**手勢 Emoji 錨定在該文字正上方**，合唱動作命中時僅進行片段染色不重複印字標記；無 `text` 的整行動作顯示於行標記列（羅馬拼音在歌詞文字下方、標記在文字上方）；`⏳3` 倒數僅顯示於該區間涵蓋的**第一行**（完整歌詞模式於類別標籤旁、卡拉OK於行上方）。完整歌詞與卡拉OK兩種模式皆然。

- 應援點位於**無歌詞間奏**或口號未命中歌詞時，該段的標記以**獨立標記列**顯示（完整歌詞與卡拉OK皆然），播放進入該點區間時該列**亮起**提示。獨立標記列中，**手勢／拍手標記置於上方一行、合唱口號以大字（比照歌詞字級）顯示於下方並染應援色**；口號文字**保留原始空格**（連續空格照常顯示，不壓縮）。完整歌詞模式的獨立標記列**標示類別標籤**（同歌詞行字級並染色）、**不顯示時間標籤**。**`⏳3` 倒數同樣適用於獨立標記列**（完整歌詞模式於類別標籤旁同排、卡拉OK於列上方）。若應援點同時覆蓋間奏與歌詞行，則間奏段顯示獨立標記列、覆蓋的歌詞行同時顯示標記。

- 標記**不去重**（片段錨定手勢標記從行標記列移除，僅在片段正上方呈現；合唱命中歌詞亦不重複標記），同一應援多次出現即多次列出，多個手勢標記對到同一片段時在片段上方依動作順序橫向並排。**沒有獨立的應援區域**。同行多類別時依「合唱 > 手勢 > 拍手」優先序，整行染色勝過部分染色。

- 31 首歌曲的資料以**手寫資料檔**維護，開發時可透過開發對齊模式直接對齊並寫回 JSON。

- App 為**響應式**，支援**手機＋平板＋桌面**，最小寬度 **320px**（含 iPhone SE 等舊小機）；平板**流式自然過渡**（不設平板專屬斷點，靠 max-width 與流式佈局自然承接，直式≈大手機、橫式≈桌面）；**iOS 安全區**（`env(safe-area-inset-*)`）套用於頂欄與底部控制列，避免被瀏海／Home indicator 遮住。字級以 `clamp()` 響應（**手機字級比桌面小一檔**，如完整歌詞歌詞本體 15px 起、卡拉OK歌詞行 20px 起）、寬度以 `min()` 自適應。驗證方式為**瀏覽器多寬度掃描**（320/375/390/768/1024/1440）＋實機確認。

- 技術棧為 **Vite + React**（純前端）；`data/` 與 `audio/` 置於 `public/` 下，以 URL 路徑引用。

- 樣式使用 **Tailwind CSS**。

- 主題為**純淺色**（Weverse 白底風格，參考 army.uubox.tw 的 ARMY READY 站），無深色模式。

- 播放支援**鍵盤快捷鍵**：`Space` 暫停/播放（不跳頁不跳句）、`←`/`→` 方向鍵前/後 5 秒跳轉、點擊歌詞行即跳轉到該行時間。

- 畫面結構為**兩個畫面**：歌曲清單頁 → 播放頁。

- **開發對齊模式以網址參數 `?dev=1` 開啟**（可置於網址 query（`/?dev=1#/song/<id>`）或 hash 後（`#/song/<id>?dev=1`），兩種皆判讀）：一般訪客網址（不含 `?dev=1`）不顯示任何開發 UI（無開發模式開關、無「音檔就緒」「已鎖定／完成率」等資訊；「缺音檔」提示仍保留給一般使用者）；網址帶 `?dev=1` 時才提供大字體 `[-5s]`~`[+5s]` 微調按鈕、`開頭秒數` 直接打字輸入框、行鎖定開關及**對齊完成率百分比**顯示。寫回 JSON 僅限本地 dev server 的 `/api/save-song-data`（GitHub Pages 部署版無此 API，訪客無法改動資料檔）。

- **播放頁頂部固定**：返回歌單、歌名與模式切換組成的頂部區塊，在完整歌詞與卡拉OK兩種模式下皆固定於畫面頂部（半透明白底＋模糊），捲動歌詞時不跑掉；演唱會提示（`note`）不固定，隨歌詞捲動。卡拉OK模式外層不捲動，歌詞容器填滿剩餘空間。

- **應援點時間不在 dev 模式提供微調 UI**：應援點（含間奏段標記）的 start/end 由資料製作流程（音檔 + ffmpeg 靜音偵測）直接產出並寫入 JSON，dev 模式僅負責歌詞行時間對齊；間奏時間偶爾不準時直接改 JSON。

- 微調結束時間時，下一句自動無縫銜接，多餘/縮短時間自動平分給後續未鎖定句子；間奏開頭秒數打字框支援手動精準設定開唱秒數，保持各句長度並同幅平移後續所有未鎖定句子（受上一句結束時間下限限制）。

- 微調時間與鎖定狀態直接透過 dev server API 即時持久化寫回 `public/data/<songId>.json`（`lockedLines` 寫回時恆維持數字由小到大升冪排序）。

- 播放頁底部控制列提供**上一首／下一首**切換，可直接跳前後首歌；並提供**Setlist 側拉歌單**：列出全部歌曲、標示目前曲、點擊即跳轉。

- **暫不做**「未練習／已完成」練習追蹤。

- 部署目標為 **GitHub Pages**（純靜態部署）。

- 音檔缺失時**清單頁標記**缺檔歌曲，**播放頁**顯示找不到音檔提示。

- App **載入時驗證**資料（重疊、手勢目錄、時間規則），錯誤顯示具體訊息。

- 合唱動作落在**無歌詞區間**合法：不塗歌詞，有補充文字則照常顯示標記。

- 歌曲播完**停止並停留在播放頁**，控制列提供「重播」按鈕。

- 播放頁版面：控制列在最下方，響應式只調比例與字體。

- **卡拉OK與完整歌詞模式皆為常駐染色**：涵蓋的歌詞行依整句／部分／未命中三態恆常染色（合唱、手勢、拍手皆然），同行多類別依「合唱 > 手勢 > 拍手」優先序，左條與類別標示同步常駐（類別標示僅完整歌詞模式；卡拉OK不顯示），不隨播放時間變化。應援標記在兩種模式下**常駐顯示**，播放進入其應援點區間時**亮起**，離開即恢復。

- 完整歌詞與卡拉OK模式皆支援**目前行淡藍背景獨立高亮**與**自動跟隨捲動**到可視範圍。

- **卡拉OK模式的「過去／目前／未來」三態以播放時間判斷**（`currentTime ≥ line.end` 為過去、落在區間內為目前、其餘為未來）：間奏（無目前行）時已唱行維持過去樣式，不重置為未來。

- 控制列含**進度條**（可拖曳）與**時間顯示**（目前秒數／總長）。

- 支援**播放速度**調整（放慢練習）。

- 有 `romaji` 的歌詞行**常駐顯示**羅馬拼音，不隨合唱區間出現消失，**無顯示開關**。

- 播放頁提供**模式切換**（`完整歌詞`／`卡拉OK`），預設為**完整歌詞模式**。

- 清單頁提供**搜尋框**，依歌名即時篩選。

- **清單頁頂部固定**：標題區塊（`Practice Library` 標籤、歌單標題與搜尋框）以 sticky 固定於畫面頂部（半透明白底＋模糊），捲動歌單時不跑掉；負 margin 抵消頂部留白，未捲動即貼齊頂部、捲動時無跳動空隙。

- **每首歌有獨立子連結**（hash 路由 `#/song/<id>`，清單頁為 `#/`）：重新整理或直接開啟網址仍停留在該歌，不跳回清單；返回、Setlist 切歌、上一首／下一首皆同步更新網址。